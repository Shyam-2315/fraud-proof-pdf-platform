services:
  - type: web
    name: pdfcraft-api
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: ./start_render.sh
    healthCheckPath: /health
    autoDeploy: false
